import 'package:flutter/material.dart';

class UserPharmacyPageScreen extends StatelessWidget {
  const UserPharmacyPageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Container(
            color: Colors.green,
          )
        ],
      ),
    );
  }
}
